extends layout

block content
    h1 Login to #{title}

    form(method='post')
        p Username:
            input(
                type= 'text'
                name= 'username'
            )
        p Password:
            input(
                type= 'password'
                name= 'password'
            )
        input(type='submit' value='Login')
            